---
homebrew_dir: "{{ '/opt/homebrew' if ansible_machine == 'arm64' else '/usr/local' }}"
homebrew_user: "{{ user }}"
homebrew_group: admin
homebrew_repo: https://github.com/Homebrew/brew.git
homebrew_taps:
  - homebrew/services
